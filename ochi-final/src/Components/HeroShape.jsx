import React from 'react';
import './HeroShape.css'; // Import the CSS file for styles

const HeroShape = () => {
    return (
        <div id="hero-shape">
            <div id="hero-1"></div>
            <div id="hero-2"></div>
            <div id="hero-3"></div>
        </div>
    );
}

export default HeroShape;
